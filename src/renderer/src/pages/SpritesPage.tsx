import React from 'react'
import { Box, Typography } from '@mui/material'

const SpritesPage: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Sprite Viewer</Typography>
    </Box>
  )
}

export default SpritesPage
