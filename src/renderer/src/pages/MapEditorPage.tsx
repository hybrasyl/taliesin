import React from 'react'
import { Box, Typography } from '@mui/material'

const MapEditorPage: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Map XML Editor</Typography>
    </Box>
  )
}

export default MapEditorPage
